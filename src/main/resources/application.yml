server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///wiki?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: wiki
    password: wiki

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.ftj.domain